/** Button to export a chart as PNG or SVG to the local filesystem. */
export default function ExportButton() {
  return (
    <button className="rounded bg-gray-800 px-4 py-2 text-sm text-white hover:bg-gray-700">
      Export Chart
    </button>
  );
}
