/** Per-second time series chart for a single ride (output, cadence, HR, etc.). */
export default function RideDetailChart() {
  return (
    <div className="rounded-lg border border-gray-200 p-6">
      <h2 className="mb-4 text-lg font-semibold">Ride Metrics</h2>
      <p className="text-gray-500">Ride detail chart will render here</p>
    </div>
  );
}
